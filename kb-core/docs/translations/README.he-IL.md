<p align="center">
  <a href="https://kb-core.com"><img src="https://raw.githubusercontent.com/safishamsi/kb_core/v4/docs/logo-text.svg" width="260" height="64" alt="KB Core"/></a>
</p>

<p align="center">
  🇺🇸 <a href="../../README.md">English</a> | 🇨🇳 <a href="README.zh-CN.md">简体中文</a> | 🇯🇵 <a href="README.ja-JP.md">日本語</a> | 🇰🇷 <a href="README.ko-KR.md">한국어</a> | 🇩🇪 <a href="README.de-DE.md">Deutsch</a> | 🇫🇷 <a href="README.fr-FR.md">Français</a> | 🇪🇸 <a href="README.es-ES.md">Español</a> | 🇮🇳 <a href="README.hi-IN.md">हिन्दी</a> | 🇧🇷 <a href="README.pt-BR.md">Português</a> | 🇷🇺 <a href="README.ru-RU.md">Русский</a> | 🇸🇦 <a href="README.ar-SA.md">العربية</a> | 🇮🇷 <a href="README.fa-IR.md">فارسی</a> | 🇮🇹 <a href="README.it-IT.md">Italiano</a> | 🇵🇱 <a href="README.pl-PL.md">Polski</a> | 🇳🇱 <a href="README.nl-NL.md">Nederlands</a> | 🇹🇷 <a href="README.tr-TR.md">Türkçe</a> | 🇺🇦 <a href="README.uk-UA.md">Українська</a> | 🇻🇳 <a href="README.vi-VN.md">Tiếng Việt</a> | 🇮🇩 <a href="README.id-ID.md">Bahasa Indonesia</a> | 🇸🇪 <a href="README.sv-SE.md">Svenska</a> | 🇬🇷 <a href="README.el-GR.md">Ελληνικά</a> | 🇷🇴 <a href="README.ro-RO.md">Română</a> | 🇨🇿 <a href="README.cs-CZ.md">Čeština</a> | 🇫🇮 <a href="README.fi-FI.md">Suomi</a> | 🇩🇰 <a href="README.da-DK.md">Dansk</a> | 🇳🇴 <a href="README.no-NO.md">Norsk</a> | 🇭🇺 <a href="README.hu-HU.md">Magyar</a> | 🇹🇭 <a href="README.th-TH.md">ภาษาไทย</a> | 🇺🇿 <a href="README.uz-UZ.md">Oʻzbekcha</a> | 🇹🇼 <a href="README.zh-TW.md">繁體中文</a> | 🇵🇭 <a href="README.fil-PH.md">Filipino</a> | 🇮🇱 <a href="README.he-IL.md">עברית</a>
</p>

<p align="center">
  <a href="https://www.ycombinator.com/companies/kb-core"><img src="https://img.shields.io/badge/Y%20Combinator-S26-F0652F?style=flat&logo=ycombinator&logoColor=white" alt="YC S26"/></a>
  <a href="https://discord.gg/598Ad9zQZ"><img src="https://img.shields.io/badge/Discord-Join-5865F2?style=flat&logo=discord&logoColor=white" alt="Discord"/></a>
  <a href="https://safishamsi.gumroad.com/l/qetvlo"><img src="https://img.shields.io/badge/Book-The%20Memory%20Layer-2ea44f?style=flat&logo=gitbook&logoColor=white" alt="The Memory Layer"/></a>
  <a href="https://github.com/safishamsi/kb_core/actions/workflows/ci.yml"><img src="https://github.com/safishamsi/kb_core/actions/workflows/ci.yml/badge.svg?branch=v8" alt="CI"/></a>
  <a href="https://pypi.org/project/kb-core/"><img src="https://img.shields.io/pypi/v/kb-core" alt="PyPI"/></a>
  <a href="https://pepy.tech/project/kb-core"><img src="https://img.shields.io/pepy/dt/kb-core?color=blue&label=downloads" alt="Downloads"/></a>
  <a href="https://github.com/sponsors/safishamsi"><img src="https://img.shields.io/badge/sponsor-safishamsi-ea4aaa?logo=github-sponsors" alt="Sponsor"/></a>
  <a href="https://www.linkedin.com/company/kb-core-labs"><img src="https://img.shields.io/badge/LinkedIn-KB Core%20Labs-0077B5?logo=linkedin" alt="LinkedIn"/></a>
  <a href="https://x.com/kb-core"><img src="https://img.shields.io/badge/X-kb-core-000000?logo=x&logoColor=white" alt="X"/></a>
</p>

<p align="center">
  <a href="https://star-history.com/#safishamsi/kb-core&Date">
    <img src="https://api.star-history.com/svg?repos=safishamsi/kb-core&type=Date" alt="Star History Chart" width="370"/>
  </a>
</p>

<div dir="rtl">

הקלידו `‎/kb-core` בעוזר ה-AI לכתיבת קוד שלכם, והוא ימפה את הפרויקט כולו — קוד, מסמכים, קובצי PDF, תמונות, סרטונים — לגרף ידע שאפשר לשאול עליו שאלות, במקום לחפש בקבצים עם grep.

עובד ב-Claude Code, Codex, OpenCode, Kilo Code, Cursor, Gemini CLI, GitHub Copilot CLI, VS Code Copilot Chat, Aider, Amp, OpenClaw, Factory Droid, Trae, Hermes, Kimi Code, Kiro, Pi, Devin CLI ו-Google Antigravity.

</div>

```
/kb-core .
```

<div dir="rtl">

זה הכול. מקבלים שלושה קבצים:

</div>

```
kb-core-out/
├── graph.html       נפתח בכל דפדפן — לחיצה על צמתים, סינון, חיפוש
├── GRAPH_REPORT.md  עיקרי הדברים: מושגי מפתח, קשרים מפתיעים, שאלות מוצעות
└── graph.json       הגרף המלא — אפשר לשאול עליו בכל רגע בלי לקרוא שוב את הקבצים
```

<div dir="rtl">

לדף ארכיטקטורה קריא עם דיאגרמות זרימת-קריאות ב-Mermaid, הריצו:

</div>

```bash
kb-core export callflow-html
```

---

<div dir="rtl">

## דרישות מקדימות

| דרישה | מינימום | בדיקה | התקנה |
|---|---|---|---|
| Python | 3.10+ | `python --version` | [python.org](https://www.python.org/downloads/) |
| uv *(מומלץ)* | כל גרסה | `uv --version` | `curl -LsSf https://astral.sh/uv/install.sh \| sh` |
| pipx *(חלופה)* | כל גרסה | `pipx --version` | `pip install pipx` |

**התקנה מהירה ב-macOS (עם Homebrew):**

</div>

```bash
brew install python@3.12 uv
```

<div dir="rtl">

**התקנה מהירה ב-Windows:**

</div>

```powershell
winget install astral-sh.uv
```

<div dir="rtl">

**Ubuntu/Debian:**

</div>

```bash
sudo apt install python3.12 python3-pip pipx
# או התקנת uv:
curl -LsSf https://astral.sh/uv/install.sh | sh
```

---

<div dir="rtl">

## התקנה

> **החבילה הרשמית:** חבילת ה-PyPI היא `kb-core` (עם y כפולה). חבילות `kb-core*` אחרות ב-PyPI אינן קשורות לפרויקט. פקודת ה-CLI היא עדיין `kb-core`.

**שלב 1 — התקנת החבילה:**

</div>

```bash
# מומלץ (סביבה מבודדת; אם הפקודה 'kb-core' לא נמצאת אחר כך, הריצו: uv tool update-shell):
uv tool install kb-core

# חלופות:
pipx install kb-core
pip install kb-core  # עשוי לדרוש הגדרת PATH — ראו הערה בהמשך
```

<div dir="rtl">

**שלב 2 — רישום המיומנות (skill) אצל עוזר ה-AI שלכם:**

</div>

```bash
kb-core install
```

<div dir="rtl">

זהו. פתחו את עוזר ה-AI והקלידו `‎/kb-core .`

כדי להתקין את המיומנות בתוך המאגר הנוכחי במקום בפרופיל המשתמש, הוסיפו `--project`:

</div>

```bash
kb-core install --project
kb-core install --project --platform codex
```

<div dir="rtl">

התקנה ברמת הפרויקט כותבת לתוך התיקייה הנוכחית, למשל `.claude/skills/kb_core/SKILL.md` או `.agents/skills/kb_core/SKILL.md` (בתוספת תיקיית `references/` שהמיומנות טוענת לפי הצורך), ומדפיסה רמז `git add` לקבצים שאפשר לבצע להם commit. פקודות פר-פלטפורמה שתומכות בהתקנה ברמת הפרויקט מקבלות את אותו דגל, למשל `kb-core claude install --project` או `kb-core codex install --project`.

> **הערת PowerShell:** השתמשו ב-`kb-core .` ולא ב-`‎/kb-core .` — הלוכסן המוביל הוא מפריד נתיבים ב-PowerShell.

> **‏`kb-core: command not found`?** ‏`uv tool install` / `pipx install` מציבים את פקודת `kb-core` בתיקיית הכלים שלהם (`~/.local/bin`). אם המעטפת (shell) לא מוצאת אותה מיד אחרי ההתקנה — נפוץ בהתקנת macOS + zsh טרייה — התיקייה הזו עדיין לא ב-`PATH`: הריצו `uv tool update-shell` (או `pipx ensurepath`) ופתחו טרמינל חדש. עם `pip` רגיל, הוסיפו את `~/.local/bin` (בלינוקס) או `~/Library/Python/3.x/bin` (במק) ל-PATH, או הריצו `python -m kb_core`.

> **מריצים עם `uvx` / `uv tool run` בלי להתקין?** ציינו את שם החבילה, לא את שם הפקודה: `uvx --from kb-core kb-core install`. ‏`uvx kb-core …` רגיל נכשל (`No solution found … no versions of kb-core`) כי `uv tool run` קורא את המילה הראשונה כשם *חבילה*, והחבילה היא `kb-core` — פקודת `kb-core` נמצאת בתוכה.

> **הימנעו מ-`pip install` במק/Windows** אם אפשר. המיומנות מאתרת את Python בזמן ריצה מתוך `kb-core-out/.kb_core_python`; אם הוא מצביע על סביבה שונה מזו שבה `pip` התקין את החבילה, תקבלו `ModuleNotFoundError: No module named 'kb-core'`. ‏`uv tool install` ו-`pipx install` מבודדים את החבילה בסביבה משלהם ונמנעים מהבעיה לחלוטין.

> **הוקים של Git עם uv tool / pipx:** ‏`kb-core hook install` מטמיע את נתיב המפרש הנוכחי ישירות בסקריפטי ההוק בזמן ההתקנה, כך שהוק ה-post-commit יופעל כראוי גם בלקוחות Git גרפיים וב-CI שבהם `~/.local/bin` אינו ב-PATH. אם התקנתם מחדש או שדרגתם את kb-core, הריצו שוב `kb-core hook install` כדי לרענן את הנתיב המוטמע.

### בחרו את הפלטפורמה שלכם

| פלטפורמה | פקודת התקנה |
|----------|----------------|
| Claude Code (לינוקס/מק) | `kb-core install` |
| Claude Code (Windows) | `kb-core install` (זיהוי אוטומטי) או `kb-core install --platform windows` |
| CodeBuddy | `kb-core install --platform codebuddy` |
| Codex | `kb-core install --platform codex` |
| OpenCode | `kb-core install --platform opencode` |
| Kilo Code | `kb-core install --platform kilo` |
| GitHub Copilot CLI | `kb-core install --platform copilot` |
| VS Code Copilot Chat | `kb-core vscode install` |
| Aider | `kb-core install --platform aider` |
| OpenClaw | `kb-core install --platform claw` |
| Factory Droid | `kb-core install --platform droid` |
| Trae | `kb-core install --platform trae` |
| Trae CN | `kb-core install --platform trae-cn` |
| Gemini CLI | `kb-core install --platform gemini` |
| Hermes | `kb-core install --platform hermes` |
| Kimi Code | `kb-core install --platform kimi` |
| Amp | `kb-core amp install` |
| Agent Skills (חוצה-פלטפורמות) | `kb-core install --platform agents` (כינוי: `--platform skills`) |
| Kiro IDE/CLI | `kb-core kiro install` |
| Pi coding agent | `kb-core install --platform pi` |
| Cursor | `kb-core cursor install` |
| Devin CLI | `kb-core devin install` |
| Google Antigravity | `kb-core antigravity install` |

משתמשי Codex צריכים גם `multi_agent = true` תחת `[features]` בקובץ `~/.codex/config.toml` לצורך חילוץ מקבילי. CodeBuddy משתמש באותו מנגנון Agent tool והוק PreToolUse כמו Claude Code. ‏Factory Droid משתמש בכלי `Task` לשיגור תת-סוכנים במקביל. OpenClaw ו-Aider משתמשים בחילוץ טורי (תמיכה בסוכנים מקביליים עדיין מוקדמת בפלטפורמות אלו). Trae משתמש ב-Agent tool לשיגור תת-סוכנים במקביל ו**אינו** תומך בהוקים מסוג PreToolUse — ‏AGENTS.md הוא המנגנון הקבוע שם.

`--platform agents` (כינוי: `--platform skills`) מכוון למיקומים הגנריים חוצי-הפלטפורמות של [Agent-Skills](https://github.com/anthropics/skills): ‏`~/.agents/skills/` הגלובלי של המשתמש (נקרא על ידי `npx skills` ומסגרות תואמות-מפרט) בהתקנה גלובלית, ו-`./.agents/skills/` בהתקנת פרויקט (`--project`). ‏`kb-core install` החשוף נשאר חד-פלטפורמי (Claude Code) בכוונה — השתמשו בפלטפורמת `agents` כשתרצו שהמיומנות תהיה זמינה לכל מסגרת שקוראת `.agents/skills`.

> Codex משתמש ב-`‎$kb-core` במקום `‎/kb-core`.

### תוספים אופציונליים

התקינו רק מה שצריך:

| תוסף | מה הוא מוסיף | התקנה |
|---|---|---|
| `pdf` | חילוץ PDF | `uv tool install "kb-core[pdf]"` |
| `office` | תמיכה ב-`.docx` ו-`.xlsx` | `uv tool install "kb-core[office]"` |
| `google` | רינדור Google Sheets | `uv tool install "kb-core[google]"` |
| `video` | תמלול וידאו/אודיו (faster-whisper + yt-dlp) | `uv tool install "kb-core[video]"` |
| `mcp` | שרת MCP stdio | `uv tool install "kb-core[mcp]"` |
| `neo4j` | דחיפה ל-Neo4j | `uv tool install "kb-core[neo4j]"` |
| `falkordb` | דחיפה ל-FalkorDB | `uv tool install "kb-core[falkordb]"` |
| `svg` | ייצוא גרף ל-SVG | `uv tool install "kb-core[svg]"` |
| `leiden` | זיהוי קהילות Leiden ‏(Python < 3.13 בלבד) | `uv tool install "kb-core[leiden]"` |
| `ollama` | הרצה מקומית עם Ollama | `uv tool install "kb-core[ollama]"` |
| `openai` | OpenAI / ממשקי API תואמי-OpenAI | `uv tool install "kb-core[openai]"` |
| `gemini` | Google Gemini API | `uv tool install "kb-core[gemini]"` |
| `anthropic` | Anthropic Claude API ‏(`--backend claude`, משתמש ב-`ANTHROPIC_API_KEY`) | `uv tool install "kb-core[anthropic]"` |
| `bedrock` | AWS Bedrock (משתמש ב-IAM, ללא מפתח API) | `uv tool install "kb-core[bedrock]"` |
| `azure` | Azure OpenAI Service ‏(`--backend azure`, משתמש ב-`AZURE_OPENAI_API_KEY` + ‏`AZURE_OPENAI_ENDPOINT`) | `uv tool install "kb-core[openai]"` |
| `sql` | חילוץ סכמות SQL | `uv tool install "kb-core[sql]"` |
| `postgres` | אינטרוספקציה של PostgreSQL חי (`--postgres DSN`) | `uv tool install "kb-core[postgres]"` |
| `dm` | חילוץ AST של BYOND DreamMaker ‏`.dm`/`.dme` (עשוי לדרוש קומפיילר C + ‏`python3-dev` אם אין wheel מתאים לפלטפורמה) | `uv tool install "kb-core[dm]"` |
| `terraform` | חילוץ AST של Terraform / HCL ‏`.tf`/`.tfvars`/`.hcl` | `uv tool install "kb-core[terraform]"` |
| `chinese` | פילוח שאילתות בסינית (jieba) | `uv tool install "kb-core[chinese]"` |
| `all` | כל מה שלמעלה | `uv tool install "kb-core[all]"` |

---

## גרמו לעוזר שלכם להשתמש בגרף תמיד

הריצו פעם אחת בפרויקט אחרי בניית גרף:

| פלטפורמה | פקודה |
|----------|---------|
| Claude Code | `kb-core claude install` |
| CodeBuddy | `kb-core codebuddy install` |
| Codex | `kb-core codex install` |
| OpenCode | `kb-core opencode install` |
| Kilo Code | `kb-core kilo install` |
| GitHub Copilot CLI | `kb-core copilot install` |
| VS Code Copilot Chat | `kb-core vscode install` |
| Aider | `kb-core aider install` |
| OpenClaw | `kb-core claw install` |
| Factory Droid | `kb-core droid install` |
| Trae | `kb-core trae install` |
| Trae CN | `kb-core trae-cn install` |
| Cursor | `kb-core cursor install` |
| Gemini CLI | `kb-core gemini install` |
| Hermes | `kb-core hermes install` |
| Kimi Code | `kb-core install --platform kimi` |
| Amp | `kb-core amp install` |
| Agent Skills (חוצה-פלטפורמות) | `kb-core agents install` (כינוי: `kb-core skills install`) |
| Kiro IDE/CLI | `kb-core kiro install` |
| Pi coding agent | `kb-core pi install` |
| Devin CLI | `kb-core devin install` |
| Google Antigravity | `kb-core antigravity install` |

הפקודה כותבת קובץ תצורה קטן שמנחה את העוזר שלכם להתייעץ עם גרף הידע בשאלות על בסיס הקוד — ולהעדיף שאילתות ממוקדות כמו `kb-core query "<שאלה>"` על פני קריאת הדוח המלא או grep על קבצים גולמיים. בפלטפורמות שתומכות בהוקים נושאי-מטען (Claude Code, ‏Gemini CLI), הוק מופעל אוטומטית לפני קריאות כלי בסגנון חיפוש (וב-Claude Code גם לפני קריאת קובצי מקור אחד-אחד דרך הכלים Read/Glob) ומכוון את העוזר לנתיב הגרף. באחרות (Codex, ‏OpenCode, ‏Cursor וכו'), קובצי ההנחיות הקבועים (`AGENTS.md`, ‏`.cursor/rules/` וכו') מספקים את אותה הנחיית "קודם הגרף". ‏`GRAPH_REPORT.md` עדיין זמין לסקירת ארכיטקטורה רחבה.

**CodeBuddy** עושה את אותם שני דברים כמו Claude Code: כותב קטע `CODEBUDDY.md` שמנחה את CodeBuddy לקרוא את `kb-core-out/GRAPH_REPORT.md` לפני מענה על שאלות ארכיטקטורה, ומתקין **הוקים מסוג PreToolUse** ‏(`.codebuddy/settings.json`) שמופעלים לפני פקודות חיפוש ב-Bash וקריאת קבצים, ומכוונים ל-`kb-core query` במקום.

**Codex** כותב ל-`AGENTS.md` וגם מתקין **הוק PreToolUse** ב-`.codex/hooks.json` שמופעל לפני כל קריאת כלי Bash — אותו מנגנון קבוע כמו ב-Claude Code.

להסרת kb-core מכל הפלטפורמות בבת אחת: `kb-core uninstall` (הוסיפו `--purge` כדי למחוק גם את `kb-core-out/`). או השתמשו בפקודה הפר-פלטפורמית (למשל `kb-core claude uninstall`).

---

**Kilo Code** מתקין את מיומנות KB Core ל-`~/.config/kilo/skills/kb_core/SKILL.md` ופקודת `‎/kb-core` נטיבית ל-`~/.config/kilo/command/kb_core.md`. ‏`kb-core kilo install` כותב גם `AGENTS.md` וגם **תוסף `tool.execute.before` נטיבי** (`.kilo/plugins/kb_core.js` + רישום ב-`.kilo/kilo.json` או `.kilo/kilo.jsonc`) כך ש-Kilo מקבל את אותה התנהגות תזכורת-גרף קבועה דרך תצורת `.kilo` נטיבית.

**Cursor** כותב `.cursor/rules/kb_core.mdc` עם `alwaysApply: true` — ‏Cursor מכליל אותו בכל שיחה אוטומטית, ללא צורך בהוק.

## מה יש בדוח

- **צומתי מפתח (God nodes)** — המושגים המקושרים ביותר בפרויקט. הכול עובר דרכם.
- **קשרים מפתיעים** — קישורים בין דברים שחיים בקבצים או מודולים שונים. מדורגים לפי מידת ההפתעה.
- **ה"למה"** — הערות בקוד (`# NOTE:`‏, `# WHY:`‏, `# HACK:`), ‏docstrings ורציונל עיצובי מהמסמכים מחולצים כצמתים נפרדים המקושרים לקוד שהם מסבירים.
- **שאלות מוצעות** — 4–5 שאלות שהגרף נמצא בעמדה ייחודית לענות עליהן.
- **תגי ביטחון** — כל קשר מוסק מסומן `EXTRACTED`‏, `INFERRED` או `AMBIGUOUS`. תמיד יודעים מה נמצא ומה נוחש.

---

## אילו קבצים הוא מטפל

| סוג | סיומות |
|------|-----------|
| קוד (36 דקדוקי tree-sitter) | `.py .ts .js .jsx .tsx .mjs .go .rs .java .c .cpp .h .hpp .cu .cuh .metal .rb .cs .kt .scala .php .swift .lua .luau .zig .ps1 .psm1 .ex .exs .m .mm .jl .vue .svelte .astro .groovy .gradle .dart .v .sv .svh .sql .f .f90 .f95 .f03 .f08 .pas .pp .dpr .dpk .lpr .inc .dfm .lfm .lpk .sh .bash .json .dm .dme .dmi .dmm .dmf .sln .slnx .csproj .fsproj .vbproj .xaml .razor .cshtml` ‏(`.dm`/`.dme` דורש `uv tool install kb-core[dm]`; ‏CUDA ‏`.cu`/`.cuh` ו-Metal ‏`.metal` משתמשים בדקדוק C++) |
| Salesforce Apex | `.cls .trigger` (מבוסס regex; מחלקות, ממשקים, enums, מתודות, טריגרים, קשתות SOQL/DML) |
| Terraform / HCL | `.tf .tfvars .hcl` (דורש `uv tool install kb-core[terraform]`) |
| תצורות MCP | `.mcp.json` ‏`mcp.json` ‏`mcp_servers.json` ‏`claude_desktop_config.json` — מחלץ צומתי שרתים, הפניות לחבילות, דרישות משתני סביבה |
| מניפסטים של חבילות | `apm.yml` ‏`pyproject.toml` ‏`go.mod` ‏`pom.xml` — צומת חבילה קנוני אחד לכל חבילה (לפי שם) בתוספת קשתות `depends_on`, כך שחבילה שמופנית מכמה מניפסטים היא מוקד (hub) יחיד |
| מסמכים | `.md .mdx .qmd .html .txt .rst .yaml .yml` (קישורי markdown ‏`[text](./other.md)` ו-`[[wikilinks]]` הופכים לקשתות `references` בין מסמכים) |
| Office | `.docx .xlsx` (דורש `uv tool install kb-core[office]`) |
| Google Workspace | `.gdoc .gsheet .gslides` ‏(opt-in; דורש אימות `gws` ו-`--google-workspace`; ‏Sheets דורש `uv tool install kb-core[google]`) |
| PDF | `.pdf` |
| תמונות | `.png .jpg .webp .gif` |
| וידאו / אודיו | `.mp4 .mov .mp3 .wav` ועוד (דורש `uv tool install kb-core[video]`) |
| YouTube / כתובות URL | כל כתובת וידאו (דורש `uv tool install kb-core[video]`) |

קוד מחולץ מקומית ללא קריאות API ‏(AST באמצעות tree-sitter). כל השאר עובר דרך ה-API של מודל עוזר ה-AI שלכם.

קובצי `.gdoc`‏, `.gsheet` ו-`.gslides` של Google Drive לשולחן העבודה הם קיצורי דרך, לא תוכן מסמכים. כדי לכלול מסמכי Google Docs, ‏Sheets ו-Slides נטיביים בחילוץ headless, התקינו ואמתו את [`gws` CLI](https://github.com/googleworkspace/cli), ואז הריצו:

</div>

```bash
uv tool install "kb-core[google]"  # נדרש לרינדור טבלאות Google Sheets
gws auth login -s drive
kb-core extract ./docs --google-workspace
```

<div dir="rtl">

אפשר גם להגדיר `KB_CORE_GOOGLE_WORKSPACE=1`. ‏kb-core מייצא קיצורי דרך אל `kb-core-out/converted/` כקובצי Markdown נלווים, ואז מחלץ אותם.

---

## פקודות נפוצות

</div>

```bash
/kb-core .                        # בניית גרף לתיקייה הנוכחית
/kb-core ./docs --update          # חילוץ מחדש של קבצים שהשתנו בלבד
/kb-core . --cluster-only         # הרצת אשכול מחדש בלי לחלץ מחדש
/kb-core . --cluster-only --resolution 1.5      # קהילות מפורטות יותר
/kb-core . --cluster-only --exclude-hubs 99     # הסתרת מוקדי-עזר מדירוג צומתי המפתח
/kb-core . --no-viz               # דילוג על ה-HTML, רק הדוח וה-JSON
/kb-core . --wiki                 # בניית ויקי markdown מהגרף
kb-core export callflow-html      # ‏HTML ארכיטקטורה/זרימת-קריאות ב-Mermaid (מתחדש אוטומטית בכל commit אם ההוק מותקן)

/kb-core query "מה מחבר את האימות למסד הנתונים?"
/kb-core path "UserService" "DatabasePool"
/kb-core explain "RateLimiter"

/kb-core add https://arxiv.org/abs/1706.03762   # הבאת מאמר והוספתו
/kb-core add <youtube-url>                       # תמלול והוספת סרטון

kb-core hook install              # בנייה מחדש אוטומטית בכל commit
kb-core merge-graphs a.json b.json              # מיזוג שני גרפים

kb-core prs                       # לוח PR: מצב CI, סטטוס ביקורת, מיפוי worktree
kb-core prs 42                    # צלילה עמוקה ל-PR ‏#42 עם השפעת גרף
kb-core prs --triage              # ‏AI מדרג את תור הביקורות שלכם (משתמש ב-backend המוגדר)
kb-core prs --conflicts           # ‏PRs שחולקים קהילות גרף — סיכון בסדר המיזוג
```

<div dir="rtl">

ראו את [רשימת הפקודות המלאה](#רשימת-הפקודות-המלאה) בהמשך.

---

## התעלמות מקבצים

צרו קובץ `.kb-coreignore` בשורש הפרויקט — אותו תחביר כמו `.gitignore`, כולל שלילה עם `!`.

**‏`.gitignore` נאכף אוטומטית.** ‏kb-core קורא את ה-`.gitignore` בכל תיקייה. אם קיים גם `.kb-coreignore`, השניים **ממוזגים** — תבניות `.kb-coreignore` מוערכות אחרונות, כך שהן גוברות במקרה של התנגשות (כולל שלילות `!`). הוספת `.kb-coreignore` רק מחריגה עוד; היא לעולם לא תחזיר קובץ שה-`.gitignore` כבר החריג. תחולת תת-תיקיות עובדת כמו ב-git — קובץ ignore משפיע רק על תת-העץ שלו.

</div>

```
# .kb-coreignore
node_modules/
dist/
*.generated.py

# לאנדקס רק את src/, להתעלם מכל השאר
*
!src/
!src/**
```

<div dir="rtl">

---

## עבודת צוות

‏`kb-core-out/` מיועד להיכנס ל-git כדי שכל חברי הצוות יתחילו עם מפה מוכנה.

**תוספות מומלצות ל-`.gitignore`:**

</div>

```
kb-core-out/cost.json        # מקומי בלבד
# kb-core-out/cache/         # אופציונלי: commit למהירות, דילוג לשמירה על ריפו קטן
```

<div dir="rtl">

> ‏`manifest.json` הוא כעת נייד — המפתחות נשמרים כנתיבים יחסיים ומעוגנים מחדש בטעינה, כך שבטוח לבצע לו commit והדבר חוסך בנייה מלאה מחדש ב-checkout הראשון.

**תהליך העבודה:**
1. אחד מחברי הצוות מריץ `‎/kb-core .` ומבצע commit ל-`kb-core-out/`.
2. כולם מושכים — העוזר שלהם קורא את הגרף מיד.
3. הריצו `kb-core hook install` לבנייה אוטומטית מחדש אחרי כל commit ‏(AST בלבד, ללא עלות API). זה גם מגדיר merge driver של git כך ש-`graph.json` לעולם לא יישאר עם סימוני קונפליקט — שני מפתחים שמבצעים commit במקביל מקבלים מיזוג-איחוד אוטומטי של הגרפים.
4. כשמסמכים או מאמרים משתנים, הריצו `‎/kb-core --update` לרענון הצמתים הללו.

---

## שימוש ישיר בגרף

</div>

```bash
# שאילתת גרף מהטרמינל
kb-core query "הצג את זרימת האימות"
kb-core query "מה מחבר בין DigestAuth ל-Response?" --graph kb-core-out/graph.json

# חשיפת הגרף כשרת MCP (לגישת כלים חוזרת)
python -m kb_core.serve kb-core-out/graph.json
python -m kb_core.serve --graph kb-core-out/graph.json  # גם הדגל --graph מתקבל

# רישום ב-Kimi Code:
kimi mcp add --transport stdio kb-core -- python -m kb_core.serve kb-core-out/graph.json

# או הגשה על HTTP כך שכל הצוות מצביע על URL אחד (בלי kb-core מקומי):
python -m kb_core.serve kb-core-out/graph.json --transport http --port 8080
python -m kb_core.serve kb-core-out/graph.json --transport http --host 0.0.0.0 --api-key "$SECRET"
```

<div dir="rtl">

שרת ה-MCP נותן לעוזר שלכם גישה מובנית: `query_graph`‏, `get_node`‏, `get_neighbors`‏, `shortest_path`‏, `list_prs`‏, `get_pr_impact`‏, `triage_prs`.

### שרת HTTP משותף

`--transport stdio` (ברירת המחדל) מריץ שרת מקומי אחד לכל מפתח. `--transport http` מגיש את אותם כלים על גבי MCP Streamable HTTP, כך שתהליך משותף אחד יכול לשרת את הגרף לכל הצוות — הלקוחות מכוונים את תצורת ה-MCP של ה-IDE אל `http://<host>:8080/mcp` במקום להריץ kb-core מקומית.

| דגל | ברירת מחדל | תפקיד |
|---|---|---|
| `--transport {stdio,http}` | `stdio` | סוג התעבורה |
| `--host` | `127.0.0.1` | כתובת ההאזנה ל-HTTP (השתמשו ב-`0.0.0.0` לחשיפה מעבר ל-localhost) |
| `--port` | `8080` | פורט ההאזנה |
| `--api-key` | משתנה סביבה `KB_CORE_API_KEY` | דרישת `Authorization: Bearer <key>` (או `X-API-Key`) |
| `--path` | `/mcp` | נתיב העיגון ב-HTTP |
| `--json-response` | כבוי | החזרת JSON רגיל במקום זרמי SSE |
| `--stateless` | כבוי | ללא מצב פר-סשן (לפריסות מאוזנות-עומס / CI) |
| `--session-timeout` | `3600` | ניקוי סשנים לא פעילים אחרי N שניות (`0` מבטל) |

ברירת המחדל `127.0.0.1` היא loopback בלבד. הגדירו `--host 0.0.0.0` **וגם** `--api-key` יחד כשחושפים על מארח משותף. אפשר להריץ בקונטיינר:

</div>

```bash
docker build -t kb-core .
docker run -p 8080:8080 -v "$(pwd)/kb-core-out:/data" kb-core \
  /data/graph.json --transport http --host 0.0.0.0 --api-key "$SECRET"
```

<div dir="rtl">

> **הערת WSL / לינוקס:** אובונטו מגיעה עם `python3`, לא `python`. השתמשו ב-venv כדי להימנע מהתנגשויות:

</div>

```bash
python3 -m venv .venv && .venv/bin/pip install "kb-core[mcp]"
```

<div dir="rtl">

---

## משתני סביבה

אלו נדרשים רק ל**חילוץ headless / CI** ‏(`kb-core extract`). בהרצה דרך מיומנות `‎/kb-core` בתוך ה-IDE, ה-API של המודל מסופק על ידי סשן ה-IDE — לא נדרשים מפתחות נוספים.

| משתנה | משמש עבור | מתי נדרש |
|---|---|---|
| `ANTHROPIC_API_KEY` | ‏backend של Claude ‏(Anthropic) | `--backend claude` |
| `ANTHROPIC_BASE_URL` | כתובת endpoint תואם-Anthropic ‏(פרוקסי LiteLLM, שערים, ...) | `--backend claude` (ברירת מחדל: `https://api.anthropic.com`) |
| `ANTHROPIC_MODEL` | שם המודל ל-backend של Claude — ל-endpoints מותאמים, השתמשו בשם/כינוי שהשרת שלכם חושף | `--backend claude` (ברירת מחדל: `claude-sonnet-4-6`) |
| `GEMINI_API_KEY` או `GOOGLE_API_KEY` | ‏backend של Google Gemini | `--backend gemini` |
| `OPENAI_API_KEY` | ‏OpenAI או ממשקים תואמי-OpenAI | `--backend openai` (שרתים מקומיים מקבלים כל ערך לא ריק) |
| `OPENAI_BASE_URL` | כתובת שרת תואם-OpenAI ‏(llama.cpp, ‏vLLM, ‏LM Studio, ...) | `--backend openai` (ברירת מחדל: `https://api.openai.com/v1`) |
| `OPENAI_MODEL` | שם המודל ל-backend של OpenAI — לשרתים בהרצה עצמית, השתמשו בשם/כינוי שהשרת חושף (בדקו את endpoint ה-`/v1/models` שלו) | `--backend openai` (ברירת מחדל: `gpt-4.1-mini`) |
| `DEEPSEEK_API_KEY` | ‏backend של DeepSeek | `--backend deepseek` |
| `MOONSHOT_API_KEY` | ‏backend של Kimi Code | `--backend kimi` |
| `OLLAMA_BASE_URL` | כתובת הרצה מקומית של Ollama | `--backend ollama` (ברירת מחדל: `http://localhost:11434`) |
| `OLLAMA_MODEL` | שם מודל Ollama | `--backend ollama` (ברירת מחדל: זיהוי אוטומטי) |
| `KB_CORE_OLLAMA_NUM_CTX` | דריסת גודל חלון ה-KV-cache של Ollama | אופציונלי — מותאם אוטומטית כברירת מחדל |
| `KB_CORE_OLLAMA_KEEP_ALIVE` | דקות להשארת מודל Ollama טעון | אופציונלי — ‏`0` לפריקה אחרי כל chunk |
| `AZURE_OPENAI_API_KEY` | ‏backend של Azure OpenAI Service | `--backend azure` |
| `AZURE_OPENAI_ENDPOINT` | כתובת ה-endpoint של משאב Azure | `--backend azure` (נדרש יחד עם מפתח ה-API) |
| `AZURE_OPENAI_API_VERSION` | דריסת גרסת ה-API של Azure | אופציונלי — ברירת מחדל `2024-12-01-preview` |
| `AZURE_OPENAI_DEPLOYMENT` או `KB_CORE_AZURE_MODEL` | שם הפריסה ב-Azure | אופציונלי — ברירת מחדל `gpt-4o` |
| `AWS_*` / `~/.aws/credentials` | ‏AWS Bedrock — שרשרת אישורים סטנדרטית | `--backend bedrock` (ללא מפתח API, משתמש ב-IAM) |
| `KB_CORE_MAX_WORKERS` | מספר threads למקביליות AST | אופציונלי — גם דגל `--max-workers` |
| `KB_CORE_MAX_OUTPUT_TOKENS` | העלאת תקרת הפלט לקורפוסים צפופים | אופציונלי — למשל `32768` לקבצים גדולים |
| `KB_CORE_API_TIMEOUT` | ‏timeout לקריאה בשניות עבור HTTP, ‏claude-cli ו-Anthropic SDK ‏(ברירת מחדל: 600) | אופציונלי — גם דגל `--api-timeout` |
| `KB_CORE_MAX_RETRIES` | מספר ניסיונות חוזרים לבקשה שנחסמה בקצב (429) לפני ויתור (ברירת מחדל: 6; מכבד `Retry-After`) | אופציונלי — העלו למגבלות ארגוניות נוקשות; ‏`0` מבטל |
| `KB_CORE_FORCE` | כפיית בנייה מחדש של הגרף גם עם פחות צמתים | אופציונלי — גם דגל `--force` |
| `KB_CORE_GOOGLE_WORKSPACE` | הפעלה אוטומטית של ייצוא Google Workspace | אופציונלי — הגדירו `1` |
| `KB_CORE_TRIAGE_BACKEND` | ‏backend עבור `kb-core prs --triage` | אופציונלי — מזוהה אוטומטית מהמפתחות הזמינים |
| `KB_CORE_TRIAGE_MODEL` | דריסת מודל לטריאז' | אופציונלי — למשל `claude-opus-4-7` |
| `KB_CORE_QUERY_LOG` | דריסת נתיב יומן השאילתות (ברירת מחדל: `~/.cache/kb-core-queries.log`) | אופציונלי — ערך ריק או `/dev/null` להשתקה |
| `KB_CORE_QUERY_LOG_DISABLE` | הגדירו `1` לביטול מוחלט של יומן השאילתות | אופציונלי |
| `KB_CORE_QUERY_LOG_RESPONSES` | הגדירו `1` לרישום גם של תשובות תת-גרף מלאות (כבוי כברירת מחדל) | אופציונלי |
| `KB_CORE_MAX_GRAPH_BYTES` | דריסת תקרת הגודל של graph.json ‏(512 MiB) — למשל `700MB`, ‏`2GB` או בייטים | אופציונלי — שימושי לקורפוסים גדולים מאוד |
| `KB_CORE_LLM_TEMPERATURE` | דריסת טמפרטורת ה-LLM לחילוץ סמנטי — למשל `0.7`, או `none` להשמטה | אופציונלי — מושמט אוטומטית למודלי היסק o1/o3/o4/gpt-5 |

---

## פרטיות

- **קובצי קוד** — מעובדים מקומית עם tree-sitter. שום דבר לא עוזב את המחשב. קורפוס של קוד בלבד אינו דורש מפתח API — ‏`kb-core extract` רץ לגמרי offline.
- **וידאו / אודיו** — מתומלל מקומית עם faster-whisper. שום דבר לא עוזב את המחשב.
- **מסמכים, PDF, תמונות** — נשלחים לעוזר ה-AI שלכם לחילוץ סמנטי (דרך מיומנות `‎/kb-core`, עם המודל שסשן ה-IDE שלכם מריץ). ‏`kb-core extract` בחילוץ headless דורש `GEMINI_API_KEY` / ‏`GOOGLE_API_KEY` ‏(Gemini), ‏`MOONSHOT_API_KEY` ‏(Kimi), ‏`ANTHROPIC_API_KEY` ‏(Claude), ‏`OPENAI_API_KEY` ‏(OpenAI), ‏`DEEPSEEK_API_KEY` ‏(DeepSeek), מופע Ollama רץ (`OLLAMA_BASE_URL`), אישורי AWS דרך שרשרת הספקים הסטנדרטית (Bedrock — ללא מפתח API, משתמש ב-IAM), או קובץ ההרצה `claude` ‏(Claude Code — ללא מפתח API, משתמש במנוי Claude שלכם). הדגל `--dedup-llm` משתמש באותו מפתח.
- **מיקום נתונים (Data residency)** — ‏`kb-core extract` מזהה אוטומטית באיזה ספק להשתמש לפי המפתח שמוגדר (עדיפות: Gemini ‏→ Kimi ‏→ Claude ‏→ OpenAI ‏→ DeepSeek ‏→ Azure ‏→ Bedrock ‏→ Ollama). לקוד עם דרישות מיקום נתונים, השתמשו ב-`--backend ollama` (מקומי לחלוטין) או העבירו דגל `--backend` מפורש. ‏Kimi ‏(`MOONSHOT_API_KEY`) מנתב לשרתי Moonshot AI בסין.
- ללא טלמטריה, ללא מעקב שימוש, ללא אנליטיקה.
- **יומן שאילתות** — כל קריאת `kb-core query`‏, `kb-core path`‏, `kb-core explain` ו-`query_graph` של MCP נרשמת ל-`~/.cache/kb-core-queries.log` בפורמט JSON Lines ‏(חותמת זמן, שאלה, קורפוס, צמתים שהוחזרו, משך). תשובות תת-גרף מלאות **אינן** נשמרות כברירת מחדל. הגדירו `KB_CORE_QUERY_LOG_DISABLE=1` לביטול, או `KB_CORE_QUERY_LOG=/dev/null` להשתקה בלי לבטל את המנגנון.

---

## פתרון בעיות

**‏`kb-core: command not found` אחרי ההתקנה**
ה-CLI מותקן אבל תיקיית ה-bin שלו אינה ב-`PATH` של המעטפת. בחרו את התיקון לפי אופן ההתקנה:
- **uv** ‏(`uv tool install kb-core`): הפקודה מגיעה לתיקיית הכלים של uv ‏(`~/.local/bin`), שהתקנת macOS/zsh טרייה לרוב לא כוללת ב-`PATH`. הריצו `uv tool update-shell` ופתחו טרמינל חדש. (מצאו את התיקייה עם `uv tool dir --bin`.)
- **pipx** ‏(`pipx install kb-core`): הריצו `pipx ensurepath` ופתחו טרמינל חדש.
- **pip** ‏(`pip install kb-core`): ‏pip מתקין סקריפטים לתיקיית bin של המשתמש שאולי אינה ב-`PATH` — הוסיפו את `~/Library/Python/3.x/bin` ‏(macOS) או `~/.local/bin` ‏(לינוקס) ל-`PATH` ב-`~/.zshrc`/`~/.bashrc`, או פשוט הריצו `python -m kb_core`.

**‏`uvx kb-core …` או `uv tool run kb-core …` לא מצליחים לפתור את `kb-core`**
חבילת ה-PyPI היא `kb-core`; ‏`kb-core` הוא רק הפקודה שהיא מספקת. ‏`uv tool run` מתייחס למילה הראשונה כשם *חבילה*, מחפש חבילה בשם `kb-core` ומדווח `No solution found … no versions of kb-core`. ציינו את החבילה במפורש: `uvx --from kb-core kb-core install` (זהה ל-`uv tool run --from kb-core kb-core install`). או התקינו פעם אחת עם `uv tool install kb-core` וקראו ל-`kb-core` ישירות.

**‏`python -m kb_core` עובד אבל פקודת `kb-core` לא**
ה-`PATH` של המעטפת לא כולל את תיקיית ה-bin שאליה הותקנה הפקודה. העדיפו `uv tool install` / ‏`pipx install` על פני `pip` רגיל, ואז הריצו `uv tool update-shell` / ‏`pipx ensurepath` ופתחו טרמינל חדש (ראו הערות ההתקנה לעיל).

**‏`‎/kb-core .` גורם ל-"path not recognized" ב-PowerShell**
‏PowerShell מתייחס ל-`/` מוביל כמפריד נתיבים. השתמשו ב-`kb-core .` (בלי לוכסן) ב-Windows.

**לגרף יש פחות צמתים אחרי `--update` או בנייה מחדש**
אם refactor מחק קבצים, הצמתים הישנים נשארים. העבירו `--force` (או הגדירו `KB_CORE_FORCE=1`) לדריסה גם כשהבנייה החדשה קטנה יותר.

**לגרף יש צמתים כפולים לאותה ישות (ghost duplicates)**
כפילויות רפאים (אותו סימבול מופיע פעמיים — פעם מחילוץ AST עם מיקום מקור, ופעם מחילוץ סמנטי בלעדיו) ממוזגות כעת אוטומטית בזמן הבנייה. אם אתם רואים זאת בגרף שנבנה לפני v0.8.33, הריצו חילוץ מלא מחדש לניקוי:

</div>

```bash
kb-core extract . --force
```

<div dir="rtl">

**‏Ollama נגמר לו ה-VRAM / חריגה מחלון ההקשר**
חלון ה-KV-cache מותאם אוטומטית אך עשוי להיות גדול מדי ל-GPU שלכם. הקטינו אותו:

</div>

```bash
KB_CORE_OLLAMA_NUM_CTX=8192 kb-core extract ./docs --backend ollama --token-budget 4000
```

<div dir="rtl">

**אזהרות `LLM returned invalid JSON` / ‏`Unterminated string`**
תשובת ה-JSON של המודל פגעה במגבלת אסימוני הפלט ונחתכה באמצע מחרוזת. ‏kb-core מתאושש אוטומטית (הוא מפצל את ה-chunk ומחלץ מחדש את החצאים, ומסמך יחיד גדול מדי נחתך תחילה בגבולות כותרות/פסקאות כך שהקובץ כולו עדיין מכוסה), כך שהאזהרות רועשות אך אין אובדן נתונים. להפחתת התופעה, העלו את תקרת הפלט או הקטינו את פלט ה-chunk:

</div>

```bash
KB_CORE_MAX_OUTPUT_TOKENS=16384 kb-core extract . --mode deep   # הרמת התקרה
kb-core extract . --mode deep --token-budget 4000                # קלטים קטנים יותר -> פלט קטן יותר
```

<div dir="rtl">

עם שער ענן כמו OpenRouter, העדיפו `--backend openai` (הגדירו `OPENAI_BASE_URL`) על פני שכבת ה-Ollama — זה נתיב תואם-OpenAI נקי יותר. אם למודל יש תקרת פלט משלו, הורדת `--token-budget` היא המנוף האמין.

**‏HTML של הגרף גדול מדי לפתיחה בדפדפן (מעל 5000 צמתים)**
דלגו על יצירת ה-HTML והשתמשו ב-JSON ישירות:

</div>

```bash
kb-core cluster-only ./my-project --no-viz
kb-core query "..."
```

<div dir="rtl">

**ל-`graph.json` יש סימוני קונפליקט אחרי ששני מפתחים ביצעו commit במקביל**
הריצו `kb-core hook install` — הוא מגדיר merge driver של git שממזג-מאחד את `graph.json` אוטומטית כך שקונפליקטים לא קורים בכלל.

**החילוץ מחזיר צמתים/קשתות ריקים למסמכים או PDF**
מסמכים, PDF ותמונות דורשים קריאת LLM — קורפוסים של קוד בלבד אינם דורשים מפתח. ודאו שמפתח ה-API מוגדר וה-backend נכון:

</div>

```bash
ANTHROPIC_API_KEY=sk-... kb-core extract ./docs --backend claude
```

<div dir="rtl">

**אזהרת חוסר התאמה בגרסת המיומנות ב-IDE**
גרסת kb-core המותקנת שונה מקובץ המיומנות. עדכנו:

</div>

```bash
uv tool upgrade kb-core
kb-core install  # דורס את קובץ המיומנות
```

<div dir="rtl">

---

## רשימת הפקודות המלאה

</div>

```
/kb-core                          # הרצה על התיקייה הנוכחית
/kb-core ./raw                    # הרצה על תיקייה מסוימת
/kb-core ./raw --mode deep        # חילוץ קשרים אגרסיבי יותר
/kb-core ./raw --update           # חילוץ מחדש של קבצים שהשתנו בלבד
/kb-core ./raw --directed         # שמירת כיוון הקשתות
/kb-core ./raw --cluster-only     # הרצת אשכול מחדש על גרף קיים
/kb-core ./raw --no-viz           # דילוג על ויזואליזציית HTML
/kb-core ./raw --obsidian         # יצירת כספת Obsidian
/kb-core ./raw --obsidian --obsidian-dir ~/vault  # כתיבה לכספת קיימת (לעולם לא דורס פתקים או תצורת .obsidian שלכם)
/kb-core ./raw --wiki             # בניית ויקי markdown שסוכנים יכולים לסרוק
/kb-core ./raw --svg              # ייצוא graph.svg
/kb-core ./raw --graphml          # ייצוא ל-Gephi / yEd
/kb-core ./raw --neo4j            # יצירת cypher.txt ל-Neo4j
/kb-core ./raw --neo4j-push bolt://localhost:7687
/kb-core ./raw --falkordb         # יצירת cypher.txt ל-FalkorDB
/kb-core ./raw --falkordb-push falkordb://localhost:6379
/kb-core ./raw --watch            # סנכרון אוטומטי כשקבצים משתנים
/kb-core ./raw --mcp              # הפעלת שרת MCP stdio

/kb-core add https://arxiv.org/abs/1706.03762
/kb-core add <video-url>
/kb-core add https://... --author "Name" --contributor "Name"

/kb-core query "מה מחבר את ה-attention לאופטימייזר?"
/kb-core query "..." --dfs --budget 1500
/kb-core path "DigestAuth" "Response"
/kb-core explain "SwinTransformer"

kb-core save-result --question "Q" --answer "A" --nodes Foo Bar --outcome useful   # תיעוד תוצאת שאלה-תשובה (זיכרון עבודה; outcome ∈ useful|dead_end|corrected)
kb-core reflect                   # איחוד תוצאות kb-core-out/memory/ אל reflections/LESSONS.md
kb-core reflect --if-stale        # לא עושה דבר אם LESSONS.md כבר חדש מכל הקלטים (זול להרצה בכל סשן)
kb-core reflect --out docs/LESSONS.md    # כתיבת מסמך הלקחים למקום אחר
kb-core reflect --graph kb-core-out/graph.json  # קיבוץ לקחים לפי קהילה + כתיבת שכבת זיכרון העבודה (.kb_core_learning.json)
                                   # השכבה מתייגת צמתים preferred/tentative/contested (משוקלל-עדכניות, עם מקור);
                                   # kb-core explain / query מציגים אז רמז "Lesson:", מסומן "code changed — re-verify" כשהמקור התקדם

kb-core uninstall                 # הסרה מכל הפלטפורמות במכה אחת
kb-core uninstall --purge         # מחיקה גם של kb-core-out/
kb-core uninstall --project --platform codex  # הסרת קובצי התקנה ברמת פרויקט בלבד

kb-core hook install              # הוקים של post-commit + post-checkout
kb-core hook uninstall
kb-core hook status

# הנחיות עוזר קבועות - פר פלטפורמה
kb-core claude install            # CLAUDE.md + הוק PreToolUse ‏(Claude Code)
kb-core claude uninstall
kb-core codebuddy install         # CODEBUDDY.md + הוק PreToolUse ‏(CodeBuddy)
kb-core codebuddy uninstall
kb-core codex install             # AGENTS.md + הוק PreToolUse ב-.codex/hooks.json ‏(Codex)
kb-core opencode install          # AGENTS.md + תוסף tool.execute.before ‏(OpenCode)
kb-core kilo install              # מיומנות Kilo נטיבית + פקודת /kb-core + ‏AGENTS.md + תוסף .kilo
kb-core kilo uninstall
kb-core cursor install            # .cursor/rules/kb_core.mdc ‏(Cursor)
kb-core cursor uninstall
kb-core gemini install            # GEMINI.md + הוק BeforeTool ‏(Gemini CLI)
kb-core gemini uninstall
kb-core copilot install           # קובץ מיומנות (GitHub Copilot CLI)
kb-core copilot uninstall
kb-core aider install             # AGENTS.md ‏(Aider)
kb-core aider uninstall
kb-core claw install              # AGENTS.md ‏(OpenClaw)
kb-core claw uninstall
kb-core droid install             # AGENTS.md ‏(Factory Droid)
kb-core droid uninstall
kb-core trae install              # AGENTS.md ‏(Trae)
kb-core trae uninstall
kb-core trae-cn install           # AGENTS.md ‏(Trae CN)
kb-core trae-cn uninstall
kb-core hermes install             # AGENTS.md + ~/.hermes/skills/ ‏(Hermes)
kb-core hermes uninstall
kb-core amp install               # קובץ מיומנות (Amp)
kb-core amp uninstall
kb-core agents install            # ~/.agents/skills/ + AGENTS.md ‏(חוצה-פלטפורמות; כינוי: kb-core skills)
kb-core agents uninstall
kb-core kiro install               # .kiro/skills/ + .kiro/steering/kb_core.md ‏(Kiro IDE/CLI)
kb-core kiro uninstall
kb-core pi install                # קובץ מיומנות (Pi coding agent)
kb-core pi uninstall
kb-core devin install             # קובץ מיומנות + .windsurf/rules/kb_core.md ‏(Devin CLI)
kb-core devin uninstall
kb-core antigravity install       # .agents/rules + .agents/workflows ‏(Google Antigravity)
kb-core antigravity uninstall

kb-core extract ./docs                        # חילוץ LLM ‏headless ל-CI ‏(ללא IDE)
kb-core extract ./docs --backend gemini       # ‏backend מפורש: gemini, kimi, claude, openai, deepseek, ollama, bedrock או claude-cli
kb-core extract ./docs --backend gemini --model gemini-3.1-pro-preview
kb-core extract ./docs --backend ollama       # ‏Ollama מקומי (הגדירו OLLAMA_BASE_URL / OLLAMA_MODEL) - ללא מפתח API ל-loopback
OPENAI_BASE_URL=http://localhost:8080/v1 OPENAI_MODEL=my-model kb-core extract ./docs --backend openai   # כל שרת תואם-OpenAI ‏(llama.cpp, vLLM, LM Studio)
ANTHROPIC_BASE_URL=http://localhost:4000 ANTHROPIC_MODEL=my-model kb-core extract ./docs --backend claude   # כל endpoint תואם-Anthropic ‏(פרוקסי LiteLLM, שערים)
KB_CORE_OLLAMA_NUM_CTX=32768 kb-core extract ./docs --backend ollama   # דריסת חלון ה-KV-cache (מותאם אוטומטית כברירת מחדל)
KB_CORE_OLLAMA_KEEP_ALIVE=0 kb-core extract ./docs --backend ollama    # פריקת המודל אחרי כל chunk (חוסך VRAM ב-GPU קטן)
kb-core extract ./docs --backend bedrock      # AWS Bedrock דרך IAM - ללא מפתח API, שרשרת אישורי AWS
kb-core extract ./docs --backend claude-cli   # ניתוב דרך Claude Code CLI - ללא מפתח API, דרך מנוי Claude שלכם
kb-core extract ./docs --backend azure        # Azure OpenAI (הגדירו AZURE_OPENAI_API_KEY + AZURE_OPENAI_ENDPOINT)
kb-core extract ./docs --max-workers 16       # מקביליות AST ‏(גם KB_CORE_MAX_WORKERS)
kb-core extract --postgres "postgresql://user:pass@host/db"   # אינטרוספקציה ישירה של סכמת PostgreSQL חיה
kb-core extract ./my-workspace --cargo        # אינטרוספקציה ישירה של תלויות Cargo workspace ברוסט
kb-core extract ./docs --token-budget 30000   # ‏chunks סמנטיים קטנים יותר למודלים מקומיים/קטנים
kb-core extract ./docs --max-concurrency 2    # פחות קריאות LLM מקביליות (שימושי להרצה מקומית)
kb-core extract ./docs --api-timeout 900      # ‏timeout ארוך יותר למודלים מקומיים איטיים (ברירת מחדל 600 שניות)
kb-core extract ./docs --google-workspace     # ייצוא .gdoc/.gsheet/.gslides דרך gws לפני החילוץ
kb-core extract ./docs --mode deep            # חילוץ סמנטי עשיר יותר עם system prompt מורחב
kb-core extract ./docs --no-cluster           # חילוץ גולמי בלבד, דילוג על אשכול
kb-core extract ./docs --timing               # הדפסת זמני ריצה פר-שלב ל-stderr ‏(עובד גם ב-cluster-only)
kb-core extract ./docs --force                # דריסת graph.json גם אם לגרף החדש פחות צמתים (אחרי refactors או לניקוי כפילויות רפאים)
kb-core extract ./docs --dedup-llm            # ‏LLM כמכריע לזוגות ישויות עמומים (משתמש באותו מפתח API)
kb-core extract ./docs --global --as myrepo   # חילוץ ורישום בגרף הגלובלי חוצה-הפרויקטים
KB_CORE_MAX_OUTPUT_TOKENS=32768 kb-core extract ./docs --backend claude  # הרמת תקרת הפלט לקורפוסים צפופים

kb-core export callflow-html                       # kb-core-out/<project>-callflow.html
kb-core export callflow-html --max-sections 8      # הגבלת מספר קטעי הארכיטקטורה שנוצרים
kb-core export callflow-html --output docs/arch.html
kb-core export callflow-html ./some-repo/kb-core-out

kb-core global add kb-core-out/graph.json --as myrepo   # רישום גרף פרויקט אל ~/.kb_core/global-graph.json
kb-core global remove myrepo                         # הסרת פרויקט מהגרף הגלובלי
kb-core global list                                  # הצגת כל המאגרים הרשומים + ספירות צמתים/קשתות
kb-core global path                                  # הדפסת הנתיב לקובץ הגרף הגלובלי

kb-core prs                              # לוח PR: ‏CI, ביקורת, worktree, השפעת גרף
kb-core prs 42                           # צלילה עמוקה ל-PR ‏#42
kb-core prs --triage                     # דירוג טריאז' AI ‏(מזהה backend אוטומטית מהסביבה)
kb-core prs --worktrees                  # מיפוי worktree ← branch ← PR
kb-core prs --conflicts                  # ‏PRs שחולקים קהילות גרף (סיכון בסדר המיזוג)
kb-core prs --base main                  # סינון ל-PRs שמכוונים ל-branch בסיס מסוים
kb-core prs --repo owner/repo            # הרצה מול מאגר GitHub אחר
KB_CORE_TRIAGE_BACKEND=kimi kb-core prs --triage   # ‏backend מסוים לטריאז'

kb-core clone https://github.com/karpathy/nanoGPT
kb-core merge-graphs a.json b.json --out merged.json
kb-core --version                                    # הדפסת הגרסה המותקנת
kb-core watch ./src
kb-core check-update ./src
kb-core update ./src
kb-core update ./src --no-cluster  # דילוג על אשכול מחדש, כתיבת גרף AST גולמי בלבד
kb-core update ./src --force       # דריסה גם אם לגרף החדש פחות צמתים
kb-core cluster-only ./my-project
kb-core cluster-only ./my-project --graph path/to/graph.json  # מיקום גרף מותאם
kb-core cluster-only ./my-project --max-concurrency 16 --batch-size 200  # תיוג קהילות מקבילי (גרפים גדולים)
kb-core cluster-only ./my-project --resolution 1.5            # יותר קהילות, קטנות יותר
kb-core cluster-only ./my-project --exclude-hubs 99           # החרגת צומתי p99 מהחלוקה
kb-core cluster-only ./my-project --no-label                  # השארת מצייני "Community N"
kb-core cluster-only ./my-project --backend=gemini            # ‏backend לשמות הקהילות
kb-core cluster-only ./my-project --backend=gemini --model gemini-2.5-pro  # מודל מסוים
kb-core label ./my-project                                    # מתן שמות (מחדש) לקהילות עם ה-backend המוגדר
kb-core label ./my-project --backend=openai --model gpt-4o   # כפיית backend ומודל מסוימים
```

<div dir="rtl">

> **שמות קהילות:** בתוך סוכן (Claude Code, ‏Gemini CLI) הסוכן נותן שמות לקהילות בעצמו. בהרצת ה-CLI החשוף, `cluster-only` נותן שמות אוטומטית עם ה-backend המוגדר (מובנה או ספק תואם-OpenAI מותאם) — העבירו `--no-label` להשארת `Community N`, או הריצו `kb-core label` ליצירת שמות מחדש לפי דרישה.

---

## ללמוד עוד

- [איך זה עובד](../how-it-works.md) — צינור החילוץ, זיהוי קהילות, ניקוד ביטחון, מדדים
- [ARCHITECTURE.md](../../ARCHITECTURE.md) — פירוק מודולים, איך מוסיפים שפה
- [אינטגרציות אופציונליות](../docker-mcp-sqlite.md) — ‏Docker MCP Toolkit + ‏SQLite

---

## נבנה על kb-core — ‏Penpax

[**Penpax**](https://kb-core.com) היא השכבה התמידית שנבנתה מעל kb-core — היא מיישמת את אותה גישת גרף על כל חיי העבודה שלכם: פגישות, היסטוריית דפדפן, מיילים, קבצים וקוד, ומתעדכנת ברציפות ברקע.

נבנתה לאנשים שהעבודה שלהם מפוזרת על פני מאות שיחות ומסמכים שהם לעולם לא יוכלו לשחזר במלואם. ללא ענן, לגמרי על המכשיר.

**גרסת ניסיון חינמית תושק בקרוב.** [הצטרפו לרשימת ההמתנה ←](https://kb-core.com)

---

<details>
<summary>תרומה לפרויקט</summary>

### הקמת סביבת פיתוח

הפרויקט משתמש ב-[uv](https://docs.astral.sh/uv/) לתהליך הפיתוח. התקינו אותו פעם אחת, ואז:

</div>

```bash
git clone https://github.com/safishamsi/kb_core.git
cd kb-core
git checkout v8                        # ‏branch הפיתוח הפעיל

# יצירת venv לפרויקט והתקנת kb-core + כל התוספים + קבוצת dev
# ‏(pytest). ‏uv מתקין את קבוצת התלויות dev כברירת מחדל; העבירו --no-dev לדילוג.
uv sync --all-extras
```

<div dir="rtl">

אימות ההתקנה במצב עריכה:

</div>

```bash
uv run kb-core --version
uv run python -c "import kb_core; print(kb_core.__file__)"
```

<div dir="rtl">

### הרצת בדיקות

</div>

```bash
uv run pytest tests/ -q                # הרצת החבילה המלאה
uv run pytest tests/test_extract.py -q # מודול אחד
uv run pytest tests/ -q -k "python"    # סינון לפי שם
```

<div dir="rtl">

> הערת macOS: חבילת הבדיקות כוללת גם `sample.f90` וגם `sample.F90`. אלו מתנגשים במערכות קבצים HFS+ / APFS שאינן רגישות לרישיות. הריצו בלינוקס או בקונטיינר Docker אם צריך לבדוק את שתי גרסאות ה-Fortran יחד.

### תהליך עבודה ב-Git

- הפיתוח הפעיל מתרחש ב-branch ‏`v8`.
- סגנון commit: ‏`fix: <description>` / ‏`feat: <description>` / ‏`docs: <description>`
- לפני פתיחת PR, הריצו `uv run pytest tests/ -q` וודאו שהוא עובר.
- הוסיפו קובץ fixture ל-`tests/fixtures/` ובדיקות ל-`tests/test_languages.py` לכל מחלץ שפה חדש.

### מה כדאי לתרום

**דוגמאות עבודה (worked examples)** הן התרומה השימושית ביותר. הריצו `‎/kb-core` על קורפוס אמיתי, שמרו את הפלט ב-`worked/{slug}/`, כתבו `review.md` כן שמכסה מה הגרף קלע ומה פספס, ופתחו PR.

**באגים בחילוץ** — פתחו issue עם קובץ הקלט, רשומת ה-cache ‏(`kb-core-out/cache/`) ומה חסר או שגוי.

ראו [ARCHITECTURE.md](../../ARCHITECTURE.md) לאחריות המודולים ואיך מוסיפים שפה.

</details>
